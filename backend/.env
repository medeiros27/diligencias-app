#-----------------------------------------------------------------------
# AMBIENTE DA APLICAÇÃO
#-----------------------------------------------------------------------
# Define o ambiente em que a aplicação está a rodar. 
# Valores comuns: 'development', 'production', 'test'
NODE_ENV=development

#-----------------------------------------------------------------------
# CONFIGURAÇÕES DO SERVIDOR
#-----------------------------------------------------------------------
# Porta em que a API será executada.
PORT=3000

# URL do frontend que terá permissão para acessar esta API.
# Em desenvolvimento, pode ser 'http://localhost:5173' (para React/Vue)
# Em produção, será o seu domínio oficial.
CORS_ORIGIN=http://localhost:5173

#-----------------------------------------------------------------------
# CONFIGURAÇÃO DO BANCO DE DADOS (POSTGRESQL)
#-----------------------------------------------------------------------
# Usar uma Connection String é o padrão moderno e mais flexível.
# Formato: postgresql://USUARIO:SENHA@HOST:PORTA/NOME_DO_BANCO
# Substitua com os seus dados reais.
DATABASE_URL="postgresql://postgres:001516@localhost:5432/diligencias"

#-----------------------------------------------------------------------
# SEGURANÇA E AUTENTICAÇÃO (JWT)
#-----------------------------------------------------------------------
# Chave secreta para assinar os tokens. DEVE SER LONGA, COMPLEXA E ÚNICA.
# Use um gerador de senhas online para criar um valor seguro.
JWT_SECRET="BRUNO_MARQUES_DE_MEDEIROS"

# Tempo de expiração do token. Exemplos: "1h", "7d", "30m"
JWT_EXPIRES_IN="30m"

#-----------------------------------------------------------------------
# LOGGING (OPCIONAL, MAS RECOMENDADO)
#-----------------------------------------------------------------------
# Nível de detalhe dos logs que serão exibidos no console.
# Valores: 'debug', 'info', 'warn', 'error'
LOG_LEVEL=debug

#-----------------------------------------------------------------------
# CONFIGURAÇÕES FUTURAS (EMAIL E ARMAZENAMENTO) - Deixe comentado por agora
#-----------------------------------------------------------------------
# Quando for implementar o envio de emails de notificação, descomente e preencha.
# MAIL_HOST=smtp.mailtrap.io
# MAIL_PORT=2525
# MAIL_USER=seu_usuario
# MAIL_PASS=sua_senha
# MAIL_FROM=nao-responda@diligencias.com

# Quando for implementar o upload de arquivos para a nuvem (ex: AWS S3).
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_S3_BUCKET_NAME=
# AWS_S3_REGION=
